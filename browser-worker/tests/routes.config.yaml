# Authoritative route list (discovery source: config). The control plane reads this file.
routes:
  - /
  - /campaigns/campaign-library
  - /campaigns/reports
  - /in-store-measurement/in-store-traffic-forecast
